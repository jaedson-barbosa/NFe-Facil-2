{
    "compilerOptions": {
        // "typeRoots": ["node_modules/@types"],  // "typeRoots": [] -> won't work
        // "types": ["node"],                      // "types": [] -> won't work
        "resolveJsonModule": true,
        "lib": ["ESNext", "DOM"]
    }
}